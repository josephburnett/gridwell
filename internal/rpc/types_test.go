package rpc

// ViewRect and its Contains/Intersects helpers were removed when tile views
// were split into kind-specific shapes (well view vs text view). The tests
// that exercised them went with them. This file is intentionally empty
// pending tests for the new types.
